(()=>{'use strict';const nav=document.getElementById('nav');const onScroll=()=>{if(window.scrollY>30)nav.classList.add('is-scrolled');else nav.classList.remove('is-scrolled')};window.addEventListener('scroll',onScroll,{passive:true});onScroll();const burger=document.querySelector('.nav__burger');const links=document.querySelector('.nav__links');burger?.addEventListener('click',()=>{const open=links.classList.toggle('is-open');burger.setAttribute('aria-expanded',String(open))});links?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('is-open');burger?.setAttribute('aria-expanded','false')}));const tabs=document.querySelectorAll('.tab');const panels=document.querySelectorAll('.menu__panel');tabs.forEach(tab=>{tab.addEventListener('click',()=>{const target=tab.dataset.tab;tabs.forEach(t=>t.classList.toggle('is-active',t===tab));panels.forEach(p=>p.classList.toggle('is-active',p.dataset.panel===target))})});const revealEls=document.querySelectorAll('.about__text, .about__media, .menu__grid, .gallery__item, .visit__info, .visit__map, .section-head');revealEls.forEach(el=>el.classList.add('reveal'));if('IntersectionObserver' in window){const io=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target)}})},{threshold:.12,rootMargin:'0px 0px -40px 0px'});revealEls.forEach(el=>io.observe(el))}else{revealEls.forEach(el=>el.classList.add('is-visible'))}const track=document.querySelector('.marquee__track');if(track)track.innerHTML+=track.innerHTML;const dot=document.querySelector('.dot');const hoursLabel=document.querySelector('.hero__hours strong');const hoursTime=document.querySelector('.hero__hours small');if(dot&&hoursLabel){const now=new Date();const day=now.getDay();const h=now.getHours();const schedule={0:[9,17],1:[8,18],2:[8,18],3:[8,18],4:[8,18],5:[8,19],6:[9,19]};const[open,close]=schedule[day];const isOpen=h>=open&&h<close;if(!isOpen){dot.style.background='#e07a5f';dot.style.boxShadow='0 0 0 4px rgba(224,122,95,.25)';hoursLabel.textContent='Nu gesloten'}if(hoursTime)hoursTime.textContent=`${String(open).padStart(2,'0')}:00 — ${String(close).padStart(2,'0')}:00`}const yearEl=document.getElementById('year');if(yearEl)yearEl.textContent=new Date().getFullYear()})();
